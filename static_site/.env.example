# Stack-wide configuration (DOMAIN, CERT_RESOLVER, SITE_NAME, brand vars, etc.)
# lives in the root .env one level up. Run `make setup` from the repo root to create it.
# The Makefile passes the root .env to this service automatically via --env-file.
