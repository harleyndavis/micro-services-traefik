{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git branch --show-current 2>/dev/null); if [ \"$branch\" = \"main\" ]; then echo \"{\\\"systemMessage\\\": \\\"WARNING: You are on the main branch. Create a feature branch before making changes: git checkout -b feature/<name>\\\"}\"; fi",
            "shell": "bash"
          }
        ]
      }
    ]
  }
}
